import React from "react";

const { default: LayoutApp } = require("../../layouts/app");

const Dashboard = () => {
  return (
    <LayoutApp>
      <p>Hi</p>
    </LayoutApp>
  );
};

export default Dashboard;
